---
title: Webhook
description: 通話イベントを自社サーバーで受信する。
---

## 開始前

**必要なもの:** `api_key` と POST を受け付ける公開 HTTPS URL。

**一般的なイベント:**

| イベント | タイミング |
| --- | --- |
| `call.started` | 通話接続 |
| `call.ended` | 通話終了 |
| `transcript.final` | 完全なトランスクリプト準備完了 |

---

<Steps>

<Step title="1. Webhook を作成">

```bash
curl -X POST http://13.207.23.185:8000/v1/webhooks \
  -H "Authorization: Bearer <api_key>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Call events",
    "url": "https://example.com/webhook",
    "events": ["call.started", "call.ended", "transcript.final"]
  }'
```


</Step>

<Step title="2. 配信をテスト">

URL へテストペイロードを送信:

```bash
curl -X POST http://13.207.23.185:8000/v1/webhooks/<webhook_id>/test \
  -H "Authorization: Bearer <api_key>"
```

サーバーログで受信リクエストを確認します。

</Step>

<Step title="3. 一覧・更新">

```bash
# 一覧
curl http://13.207.23.185:8000/v1/webhooks \
  -H "Authorization: Bearer <api_key>"

# イベント更新
curl -X PATCH http://13.207.23.185:8000/v1/webhooks/<webhook_id> \
  -H "Authorization: Bearer <api_key>" \
  -H "Content-Type: application/json" \
  -d '{
    "events": ["call.started", "call.ended"]
  }'

# 削除
curl -X DELETE http://13.207.23.185:8000/v1/webhooks/<webhook_id> \
  -H "Authorization: Bearer <api_key>"
```

</Step>

</Steps>

---

## API リファレンス

[Webhook 作成](/api-reference/webhooks/create-webhook) · [Webhook テスト](/api-reference/webhooks/test-webhook-delivery) · [Webhook 一覧](/api-reference/webhooks/list-webhooks)
